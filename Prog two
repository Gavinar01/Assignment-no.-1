using System;

class Program
{
    static void Main()
    {
        for (int i = 1; i <= 100; i++)
        {
            Console.WriteLine(i % 3 == 0 ? (i % 5 == 0 ? "Hello Goodbye" : "Hello") : i % 5 == 0 ? "Goodbye" : i);
        }
    }
}
